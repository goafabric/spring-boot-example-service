kind: Service
apiVersion: v1
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx-service
  ports:
    - protocol: TCP
      port: 8080
      nodePort: 30080 #this is the external port
  type: NodePort