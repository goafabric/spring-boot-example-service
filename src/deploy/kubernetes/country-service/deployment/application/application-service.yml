kind: Service
apiVersion: v1
metadata:
  name: country-service
spec:
  selector:
    app: country-service
  ports:
    - protocol: TCP
      port: 50700
      nodePort: 30700 #this is the external port
  type: NodePort