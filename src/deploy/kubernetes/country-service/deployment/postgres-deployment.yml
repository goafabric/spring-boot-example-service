apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-postgres
spec:
  selector:
    matchLabels:
      app: example-postgres
  replicas: 1
  strategy:
    type: Recreate #RollingUpdate
  template:
    metadata:
      labels:
        app: example-postgres
    spec:
      containers:
        - name: example-postgres
          image: postgres:12.4
          imagePullPolicy: IfNotPresent #Always
          ports:
            - containerPort: 5432
          args:
            - postgres
            - -c
            - shared_buffers=128MB
            - -c
            - max_connections=30
            - -c
            - shared_preload_libraries=pg_stat_statements
          envFrom:
            - secretRef:
                name: example-postgres-secret
          volumeMounts:
            - mountPath: /docker-entrypoint-initdb.d/
              name: example-postgres-initdb-volume
            - mountPath: /var/lib/postgresql/data
              name: example-postgres-pvc-volume

      volumes:
        - name: example-postgres-initdb-volume
          configMap:
            name: example-postgres-initdb
        - name: example-postgres-pvc-volume
          persistentVolumeClaim:
            claimName: example-postgres-pvc