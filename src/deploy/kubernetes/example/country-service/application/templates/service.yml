kind: Service
apiVersion: v1
metadata:
  name: country-service-application
spec:
  selector:
    app: country-service-application
  ports:
    - protocol: TCP
      port: 50700
      nodePort: 30970 #this is the external port
  type: NodePort