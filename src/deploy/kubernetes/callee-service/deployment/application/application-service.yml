kind: Service
apiVersion: v1
metadata:
  name: callee-service
spec:
  selector:
    app: callee-service
  ports:
    - protocol: TCP
      port: 50900
      nodePort: 30900 #this is the external port
  type: NodePort