apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-welcome-application-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /$2

spec:
  tls:
    - hosts:
        - goafabric.org
      secretName: server-certificate-secret
  rules:
    - host: goafabric.org
      http:
        paths:
          - path: /welcome(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: nginx-welcome-application
                port:
                  number: 8080