- Better SSL Certificate
- Jasypt
- upgrad auf keycloak 8
- rest client

keytool -genkey -keyalg RSA -alias selfsigned -keystore keystore.jks -storepass password -validity 360 -keysize 2048

Keycloak)
- https://www.baeldung.com/spring-boot-keycloak
    - realm: goafabric
    - client: login-app
        - valid redirect urls: *
    - settings/roles
        - add role STANDARD_ROLE
    - manage/users
        - add user admin
        - add role STANDARD_ROLE
        
- docker run -d --name=keycloak -p 8080:8080 -e KEYCLOAK_USER=admin -e KEYCLOAK_PASSWORD=admin jboss/keycloak:8.0.1

- Multi Tenancy: https://www.czetsuyatech.com/2020/11/how-to-implement-multitenancy-with-spring-boot-and-keycloak.html
=> PathBaseContextResolver could than directly to requst.getHeader(getTenantId())
=> Also see official Keycloak: https://www.keycloak.org/docs/latest/securing_apps/#_multi_tenancy